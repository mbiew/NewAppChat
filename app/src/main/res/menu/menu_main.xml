<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_logout"
        android:title="Logout"/>

    <item android:id="@+id/action_search"
        android:title="Search"
        android:icon="@drawable/ic_baseline_search"
        app:showAsAction="always"
        app:actionViewClass="android.widget.SearchView" />

    <!-- show this option in groupChat fragment, user fragment and hide from other -->
    <item android:id="@+id/action_create_group"
        android:title="Create Group"
        app:showAsAction="always"
        android:icon="@drawable/ic_baseline_group_add_white"/>

    <item android:id="@+id/action_add_participant_group"
        android:title="Add Participant Group"
        app:showAsAction="always"
        android:icon="@drawable/ic_baseline_person_add"/>

    <item android:id="@+id/action_groupinfo"
        android:title="Group Info"
        app:showAsAction="always"
        android:icon="@drawable/ic_baseline_info"/>



</menu>